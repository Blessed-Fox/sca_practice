
Y = mx + b

Y = response
m = slope
x = explanatory
b = offset

stats specific
y = a + bx
	a = mean(y) - b(mean(x))

b = r(sy/sx)


sx = standard deviation(x)
sy = standard deviation(y)

y = (mean(y) - b(mean(x)) + (r(sy/sx)x))